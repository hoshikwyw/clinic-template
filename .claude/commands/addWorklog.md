---
description: End the session and append a dated entry to docs/WORKLOG.md
---

The user has ended this working session. Append a new dated entry to the TOP of
`docs/WORKLOG.md` (newest first), following the template at the bottom of that file.

Steps:
1. Read `docs/WORKLOG.md` to see existing entries and the template.
2. Determine today's date (use the `currentDate` from the session context, format `YYYY-MM-DD`).
   If an entry for today already exists, append to / update it rather than duplicating.
3. Write a concise but complete entry covering this session:
   - **Session focus** — one line.
   - **Decisions made** — concrete decisions, locked choices.
   - **Done / changed** — files created or edited, work completed.
   - **Still open** — unresolved questions (keep in sync with `docs/07-open-decisions.md`).
   - **Next session should** — clear starting point for next time.
4. Insert it directly below the journal header, ABOVE the most recent prior entry.
5. Confirm to the user with a one-line summary of what you logged.

Base the content on what actually happened in THIS conversation — do not invent work.
