/**
 * telehealth module — vertical slice (components/ + server/ + schema + types colocated).
 *
 * Video consultations. Toggleable per clinic.
 *
 * Toggleable via ClinicConfig.modules.telehealth. Public API of the module is this file.
 * See docs/02-architecture.md ("Feature Modules") and docs/03-folder-structure.md.
 * NOTE: Phase 0 stub.
 */

export {};
