/**
 * notifications module — vertical slice (components/ + server/ + schema + types colocated).
 *
 * Appointment reminders via email (Resend) / SMS / native+web push.
 *
 * Toggleable via ClinicConfig.modules.notifications. Public API of the module is this file.
 * See docs/02-architecture.md ("Feature Modules") and docs/03-folder-structure.md.
 * NOTE: Phase 0 stub.
 */

export {};
