/**
 * patients module — vertical slice (components/ + server/ + schema + types colocated).
 *
 * Patient records, profiles, intake (rendered via form-engine).
 *
 * Toggleable via ClinicConfig.modules.patients. Public API of the module is this file.
 * See docs/02-architecture.md ("Feature Modules") and docs/03-folder-structure.md.
 * NOTE: Phase 0 stub.
 */

export {};
